# DiaROS Docker Environment Variables

# Display settings for GUI applications (macOS/Linux)
# For macOS: Use XQuartz and set DISPLAY=host.docker.internal:0
# For Linux: Usually DISPLAY=:0 or DISPLAY=:1
DISPLAY=:0

# ROS2 Domain ID (0-232)
# Use different IDs to isolate ROS2 networks
ROS_DOMAIN_ID=0

# ROS2 localhost only mode (0 or 1)
# Set to 1 to restrict ROS2 communication to localhost only
ROS_LOCALHOST_ONLY=0

# Docker compose project name
COMPOSE_PROJECT_NAME=diaros